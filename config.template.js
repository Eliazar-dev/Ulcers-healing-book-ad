/* ========= CONFIG — EDIT THESE ========= */
const CONFIG = {
  whatsappNumber: "254111646402",
  whatsappMessage: "Hi, I'd like to order The Natural Ulcer Cure book.",
  adminPasscode: "gemsofinsight2026",

  // Only needed if you deploy this page outside Claude (your own domain).
  // Leave both as-is while previewing inside Claude — tracking works automatically there.
  // See the deployment guide for how to create these two values (free, ~5 minutes).
  supabaseUrl: "__SUPABASE_URL__",
  supabaseAnonKey: "__SUPABASE_ANON_KEY__"
};
